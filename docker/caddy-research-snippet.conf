# Caddy config for research.erwarx.com
# Add this to /etc/caddy/Caddyfile on the VPS

research.erwarx.com {
    reverse_proxy localhost:8089
    
    header {
        X-Content-Type-Options nosniff
        X-Frame-Options SAMEORIGIN
        -Server
    }
}
